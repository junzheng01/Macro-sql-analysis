##  CREATE NORMALIZED TABLES (Do This via SQL Editor)

Once the raw data is uploaded, open SQL Editor and paste:

###  A. countries table

```sql
CREATE TABLE "countries" (
  "ISO3" TEXT PRIMARY KEY,
  "countryname" TEXT NOT NULL
);

### B. years table
CREATE TABLE "years" (
  "year" INT PRIMARY KEY
);

### C. macro_indicators table (normalized)

CREATE TABLE "macro_indicators" (
  "ISO3" TEXT,
  "year" INT,

   "nGDP" NUMERIC,
    "rGDP" NUMERIC,
    "rGDP_pc" NUMERIC,
    "rGDP_USD" NUMERIC,
    "deflator" NUMERIC,

    "cons" NUMERIC,
    "rcons" NUMERIC,
    "cons_GDP" NUMERIC,

    "inv" NUMERIC,
    "inv_GDP" NUMERIC,
    "finv" NUMERIC,
    "finv_GDP" NUMERIC,

    "exports" NUMERIC,
    "exports_GDP" NUMERIC,
    "imports" NUMERIC,
    "imports_GDP" NUMERIC,

    "CA" NUMERIC,
    "CA_GDP" NUMERIC,
    "USDfx" NUMERIC,
    "REER" NUMERIC,

    "govexp" NUMERIC,
    "govexp_GDP" NUMERIC,
    "govrev" NUMERIC,
    "govrev_GDP" NUMERIC,
    "govtax" NUMERIC,
    "govtax_GDP" NUMERIC,
    "govdef" NUMERIC,
    "govdef_GDP" NUMERIC,
    "govdebt" NUMERIC,
    "govdebt_GDP" NUMERIC,

    "HPI" NUMERIC,
    "CPI" NUMERIC,
    "infl" NUMERIC,

    "pop" BIGINT,
    "unemp" NUMERIC,
    "strate" NUMERIC,
    "ltrate" NUMERIC,
    "cbrate" NUMERIC,

    "M0" NUMERIC,
    "M1" NUMERIC,
    "M2" NUMERIC,
    "M3" NUMERIC,
    "M4" NUMERIC,

    "nGDP_USD" NUMERIC,
    "cons_USD" NUMERIC,
    "inv_USD" NUMERIC,
    "finv_USD" NUMERIC,
    "imports_USD" NUMERIC,
    "exports_USD" NUMERIC

  PRIMARY KEY ("ISO3", "year"),
  FOREIGN KEY ("ISO3") REFERENCES "countries" ("ISO3"),
  FOREIGN KEY ("year") REFERENCES "years" ("year")
);


###  D. crisis_events table
CREATE TABLE "crisis_events" (
  "event_id" SERIAL PRIMARY KEY,
  "ISO3" TEXT,
  "year" INT,
  "crisis_type" TEXT CHECK ("crisis_type" IN ('Banking', 'Currency', 'Sovereign')),
  FOREIGN KEY ("ISO3") REFERENCES "countries" ("ISO3"),
  FOREIGN KEY ("year") REFERENCES "years" ("year")
);



